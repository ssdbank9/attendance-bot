# Credentials - NEVER push
config.json

# Python
__pycache__/
*.pyc
*.pyo
.venv-dashboard/
.dashboard_auth_token

# Logs
timein_logs/
*.log

# Local attendance database - source of truth stays on the desktop;
# timein_status.json/timein_history.json (regenerated from it) are what
# gets synced instead.
attendance.db
attendance.db-*
*.pre-migration-backup

# Local HTML page and assets
timein_page.html
timein_page_files/

# Backups
cloud_backup/

# Static assets (served locally only)
static/

# Editor / OS
.vscode/
*.swp
*~
Thumbs.db
Desktop.ini

# Scheduled-task definitions exported before the hide-the-window change
task_backup/
